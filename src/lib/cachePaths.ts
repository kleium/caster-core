/**
 * Back-compat re-export. The canonical definition now lives in `paths.ts`,
 * which resolves everything inside this package rather than reaching up into
 * the old monorepo layout.
 */
export { CACHE_DIR } from './paths.js';
